spring:
    datasource:
        username: root
        url: jdbc:mysql://localhost:3306/db-person-crud
        driver-class-name: com.mysql.cj.jdbc.Driver
jpa:
    show-sql: true
    hibernate:
        ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
server:
    port: 8080